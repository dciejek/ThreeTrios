package model;

import java.awt.Color;

/**
 * Represents the possible colors for a player to have.
 */
public enum PlayerColor {
  BLUE(Color.BLUE),
  RED(Color.RED);

  PlayerColor(Color color) {}
}
