package provider.model;

/**
 * The CellType enum represents the types of cells that can exist within the game grid in
 * the Three Trios game. Each cell type defines a specific purpose or state for the cell.
 */
public enum CellType {
  HOLE,
  CARD
}
